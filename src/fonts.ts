// Typefaces served with the app (SIL Open Font License 1.1, packaged by
// Fontsource) instead of from Google Fonts, so that loading the page sends no
// request to a third party. Each file covers the subsets of one face; the
// browser downloads only the subsets (unicode-range) that the page uses.
import '@fontsource-variable/fraunces/opsz.css'
import '@fontsource-variable/hanken-grotesk/wght.css'
import '@fontsource/dm-mono/400.css'
import '@fontsource/dm-mono/500.css'
import '@fontsource-variable/stix-two-text/wght.css'
import '@fontsource-variable/stix-two-text/wght-italic.css'
